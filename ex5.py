import datetime
age = 10
name = 'rahul'
date = datetime.date.today()
print repr(date)
print "name: %s and age: %d" % (name, age)
print "name: %r and age: %d and date is: %r" % (name, age, date)
print round(1.343)